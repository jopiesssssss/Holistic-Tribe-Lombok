Kuara gallery patch v6

Upload this patch into the root of your Holistic Tribe Lombok GitHub repository and overwrite existing files.

Changes:
- Gallery moved directly underneath the hero.
- Visible Booking-style collage adjusted so the smaller photos are no longer narrow/portrait-like.
- The beach-house/ocean-view image is now immediately visible as the main gallery image.
- The Show all photos modal and large-photo viewer still work.
- Thumbnail files are included again, so this patch can be uploaded over patch 4 or patch 5.
