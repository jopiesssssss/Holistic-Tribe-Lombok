Holistic Tribe Lombok — facilitators show-more patch

Changed files:
- index.html
- assets/js/main.js
- assets/css/styles.css
- data/site-data.js
- data/facilitators.json

What changed:
1. Kuara Resort Lombok is now the first facilitator in the data, so it appears top-left on the homepage and on the all-facilitators page.
2. The homepage no longer places “View all facilitators” in the top-right of the section.
3. A clear “See more facilitators” button now appears underneath the first 6 facilitator cards. It expands the remaining facilitators inline.
4. A small “Open full directory with search, filters and map” link remains underneath for the dedicated facilitator page.
5. Kuara image paths are now root-relative, so the card image also works on nested pages like /facilitators/.
6. Added a scroll offset and a subtle sticky-header background so content does not visually sit awkwardly behind the navigation bar.

Upload/replace these files in GitHub and deploy.
